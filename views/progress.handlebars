{{> nav/nav-block }}

<div class="text-center">
    <h3 class="white-text mb-5 mt-4 font-weight-bold"><strong>Foxy</strong> <a
            class="green-text font-weight-bold"><strong> The Fixer</strong></a></h3>
    <h1>Game Page</h1>
    <h2>Let's GO!!!!</h2>
            {{#each progress}}
                <p>For {{points}} points:</p>
                <h2 question_id={{id}}>{{question}}</h2>
                <button>{{correct_answer}}</button>
                <button>{{wrong_answer_1}}</button>
                <button>{{wrong_answer_2}}</button>
            {{/each}}
</div>